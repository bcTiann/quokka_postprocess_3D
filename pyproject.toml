[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quokka2s"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "quokka2s/src"}

[tool.setuptools.packages.find]
where = ["quokka2s/src"]
